<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="snippets.list">
      <field name="name">snippets list</field>
      <field name="model">snippets.snippets</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="snippets.action_window">
      <field name="name">snippets window</field>
      <field name="res_model">snippets.snippets</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="snippets.action_server">
      <field name="name">snippets server</field>
      <field name="model_id" ref="model_snippets_snippets"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="snippets" id="snippets.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="snippets.menu_1" parent="snippets.menu_root"/>
    <menuitem name="Menu 2" id="snippets.menu_2" parent="snippets.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="snippets.menu_1_list" parent="snippets.menu_1"
              action="snippets.action_window"/>
    <menuitem name="Server to list" id="snippets" parent="snippets.menu_2"
              action="snippets.action_server"/>
    -->
  </data>
</odoo>